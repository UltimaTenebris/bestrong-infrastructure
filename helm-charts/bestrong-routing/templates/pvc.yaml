apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bestrong-shared-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  resources: 
    requests:
      storage: {{ .Values.size }}